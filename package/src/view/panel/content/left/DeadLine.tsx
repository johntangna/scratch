import { h, Component } from 'preact';
import { observer } from '../../../..';

type IProps = {

};

@observer
class DeadLine extends Component<IProps> {
  render() {
    return (
      <div />
    );
  }
}

export default DeadLine;
